---
description: Plan a feature before coding — design, files to change, verification.
---

Plan the following work before writing any code: **$ARGUMENTS**

Use the `plan-feature` skill (or delegate to the `planner` subagent for larger explorations).
Explore existing code for reuse first, then output: Goal → Assumptions → Approach → Files to change
→ Verification. Do not implement yet — produce the plan and stop for my approval.
