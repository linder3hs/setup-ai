---
description: Scaffold a new component, route, module, or project skeleton.
---

Scaffold: **$ARGUMENTS**

Use the `scaffold-project` skill (or the `scaffolder` subagent). Read sibling files to match the
project's conventions first, generate only the needed files (the thing, its types, a test stub, and
the index/export wiring), and leave it runnable — no unresolved imports or build-breaking TODOs.
Report the files created and how to reach the new thing.
